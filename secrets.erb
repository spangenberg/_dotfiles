# Set your git user info
export GIT_AUTHOR_NAME='<%= print 'Your git author name: '; STDOUT.flush; STDIN.gets.chomp %>'
export GIT_AUTHOR_EMAIL='<%= print 'Your git author email: '; STDOUT.flush; STDIN.gets.chomp %>'
export GIT_COMMITTER_NAME='<%= print 'Your git committer name: '; STDOUT.flush; STDIN.gets.chomp %>'
export GIT_COMMITTER_EMAIL='<%= print 'Your git committer name: '; STDOUT.flush; STDIN.gets.chomp %>'

# Optionally, set your GitHub credentials
export GITHUB_USER='<%= print 'Your github user: '; STDOUT.flush; STDIN.gets.chomp %>'
export GITHUB_TOKEN='<%= print 'Your github token: '; STDOUT.flush; STDIN.gets.chomp %>'
